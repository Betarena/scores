# ╭──────────────────────────────────────────────────────────────────╮
# │ 🐙 GITHUB ACIONS CI/CD WORKFLOW                                  │
# ╰──────────────────────────────────────────────────────────────────╯

name: '♻️ sync labels'

on:
  # ╭─────
  # │ NOTE:
  # │ |: Debugging / Testing only
  # ╰─────
  # push:
  #   branches:
  #     - chore/miscellenous/draft/3
  # ╭─────
  # │ NOTE:
  # │ |: Manual trigger from GitHub UI
  # ╰─────
  workflow_dispatch:
#

jobs:

  repository-auto-label-sync:
    name: ♻️ JOB - Sync labels
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: srealmoreno/label-sync-action@v2
        # with:
        # ╭─────
        # │ NOTE: WARNING:
        # │ |: This will delete any labels not defined in the config file.
        # ╰─────
        #   clean-labels: true
  #
#