# ╭──────────────────────────────────────────────────────────────────╮
# │ 🐙 GITHUB ACIONS CI/CD WORKFLOW                                  │
# ╰──────────────────────────────────────────────────────────────────╯

name: '📬 Open'

on:
  pull_request:
    branches:
      - main
    types:
      - opened
#

jobs:

  pull-request-comment-insert:
    name: '📝 JOB /:/ PR add comment'
    runs-on: ubuntu-22.04
    steps:
      - uses: mshick/add-pr-comment@v1
        with:
          message: |
            > [!IMPORTANT]
            > Please set a **single** label , of type **`semver:[patch|minor|major]`** to **THIS** `Pull-Request`.
            >
            > If you are not sure which label to apply, please ask for help from a team member.

            > [!NOTE]
            > **Ignore this message if this is `pull-request` does _NOT_ involve a change of type `feature` OR `fix`.**
          repo-token: ${{ secrets.GITHUB_TOKEN }}
  #
#