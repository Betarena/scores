# Runbook — scores

## Commands (confirm in package.json)
- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`
- Lint: `npm run lint`
- Test: `npm run test` (or repo test command)

## Env
- Env files live under `env/` (do not commit secrets).
