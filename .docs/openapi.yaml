openapi: 3.0.0

info:
  title: Betarena | Scores Platform Endpoints
  description:
    "These endpoints are avaialble on the <b>Betarena | Scores Platform</b> for consumption
    as an internal API endpoints, for a means to communicate with linked data endpoints
    with this application."
  version: 0.1.0

servers:
  # LOCAL NETWORK
  - url: http://localhost:3050
    description: Server [LOCAL] environment
  # DEVELOPMENT
  - url: https://betarena-scores-platform.herokuapp.com/
    description: Server [DEV] environment
  # PRODUCTION
  - url: https://scores.betarena.com/
    description: Server [PROD] environment

paths:

  /auth/siwe:
    post:
      tags:
        - Auth
      summary:
        Exchange a SIWE signature for a Firebase custom token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  description: Signed SIWE message
                signature:
                  type: string
                  description: Signature from the wallet
              required:
                - message
                - signature
      responses:
        '200':
          description: Firebase custom token minted

  # ===================
  # LAYOUT
  # ===================

  # [L] - SEO-PAGES
  /api/data/main/seo-pages:
    get:
      tags:
        - SEO + Sitemap

      summary:
        Returns a targets' player main SEO widget (data).

      description:
        Optional extended description in CommonMark or HTML.

      parameters:
        - name: filter
          in: query
          description:
            Target player (main) SEO widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              ### NOTE:
              ### ?lang=[...]
              lang:
                type: string
                example: en
              ### NOTE:
              ### ?term=[...]
              term:
                type: string
                example: football
                enum:
                  - football
                  - competitions
                  - player
                  - team
              ### NOTE:
              ### ?player_id=[...]
              player_id:
                type: number
                example: 205881
              ### NOTE:
              ### ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18548156
              ### NOTE:
              ### ?player_id=[...]
              competition_id:
                type: number
                example: 31
              ### NOTE:
              ### ?page=[...]
              page:
                type: string
                enum:
                  - fixtures
                  - player
                  - competitions
                  - competition
                example: player
              ### NOTE:
              ### ?hasura=[...]
              hasura:
                type: boolean
                example: true
                enum:
                  - true
                  - false
              ### NOTE:
              ### ?countires=[...]
              countires:
                type: boolean
                example: true
                enum:
                  - true
                  - false
            additionalProperties: false
            oneOf:
              # ### NOTE:
              # ### Data for Competitions.
              - required: [lang, page]
              # ### NOTE:
              # ### Data for Fixture.
              - required: [fixture_id, page]
              - required: [fixture_id, page, hasura]
              # ### NOTE:
              # ### Data for Player.
              - required: [player_id, page]
              - required: [player_id, page, hasura]
              # ### NOTE:
              # ### Data for Competition.
              - required: [competition_id, page]
              - required: [competition_id, page, hasura]
              # ### NOTE:
              # ### Data for Translations (terms).
              - required: [term]
              - required: [countires]

      responses:
        200:
          description:
            Target player SEO widget data

  # [L] - NAVBAR Widget
  /api/data/main/navbar:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target footer (main) FOOTER widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation
              - required: [lang]
              - required: [lang, hasura]
      summary:
        Navbar [C][H]
      responses:
        200:
          description:
            Successful NAVBAR data fetch

  # [L] - FOOTER Widget
  /api/data/main/footer:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target footer (main) FOOTER widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation
              - required: [lang]
              - required: [lang, hasura]
      summary:
        Footer [C][H]
      responses:
        200:
          description:
            Successful FOOTER data fetch

  # [L] - SPORTBOOK Widget
  /api/data/main/sportbook:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target footer (main) FOOTER widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?all=[...]
              all:
                type: boolean
                example: true
              # ?geoPos=[...]
              geoPos:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Data 1
              - required: [all, geoPos]
              - required: [all, geoPos, hasura]
              # [MAIN] Data 2
              - required: [geoPos]
              - required: [geoPos, hasura]

      summary:
        Footer [C][H]

      responses:
        200:
          description:
            Successful FOOTER data fetch

  # [L] - USERGUIDE Widget
  /api/data/main/userguide:
    get:

      summary:
        (MISC) Userguide

      parameters:
        - name: filter
          in: query
          description:
            Endpoint for **Userguide** data retrieval and handling.
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?userguideId=[...]
              userguideId:
                type: number
                example: 1
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Data
              - required: [userguideId, lang]
              - required: [userguideId, lang, hasura]

      responses:
        200:
          description:
            Successful data retrieval.

  # ◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️
  # HOMEPAGE                        ◼️
  # ◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️◼️

  # [HP] - FEAT MATCH Widget
  /api/data/home/feat-match:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?geoPos=[...]
              geoPos:
                type: string
                example: es
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Geo-Positional Data
              - required: [geoPos]
              - required: [geoPos, hasura]
              # [MAIN] SEO Data
              - required: [lang, seo]
              - required: [lang, seo, hasura]
      summary:
        Featured Match [C][H]
      responses:
        200:
          description:
            Target fixture FEAT-MATCH widget data

  # [HP] - FEAT BETSITE Widget
  /api/data/home/feat-betsite:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-BETSITE widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation
              - required: [lang]
              - required: [lang, hasura]
      summary:
        Featured Betsite [C][H]
      responses:
        200:
          description:
            Target fixture FEAT-BETSITE widget data

  # [HP] - LEAGUE-LIST Widget
  /api/data/home/league-list:
    get:

      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?geoPos=[...]
              geoPos:
                type: string
                example: es
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Geo-Positional Data
              - required: [geoPos]
              - required: [geoPos, hasura]
              # [MAIN] SEO Data
              - required: [lang, seo]
              - required: [lang, seo, hasura]

      summary:
        League List [C][H]

      responses:
        200:
          description:
            Target fixture FEAT-MATCH widget data

  # [HP] - LEAGUE-TABLE Widget
  /api/data/home/league-table:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?geoPos=[...]
              geoPos:
                type: string
                example: es
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Geo-Positional Data
              - required: [geoPos]
              - required: [geoPos, hasura]
      summary:
        League Table [C][H]
      responses:
        200:
          description:
            Target LEAGUE-TABLE widget data

  # [HP] - SEO-BLOCK Widget
  /api/data/home/seo-block:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
      summary:
        Seo-Block [C][H]
      responses:
        200:
          description:
            Target SEO-BLOCK widget data

  # [HP] - TOP-GOALSCORER Widget
  /api/data/home/top-goalscorers:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?geoPos=[...]
              geoPos:
                type: string
                example: es
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Geo-Positional Data
              - required: [geoPos]
              - required: [geoPos, hasura]
              # [MAIN] SEO Data
              - required: [lang, seo]
              - required: [lang, seo, hasura]
      summary:
        Top Goalscorers [C][H]
      responses:
        200:
          description:
            Target LEAGUE-TABLE widget data

  # ===================
  # LEAGUE PAGE
  # ===================

  # [LP] - LEAGUE-INFO Widget
  /api/data/league/info:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target home (main) FEAT-MATCH widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?lang=[...]
              url:
                type: string
                example: "/football/spain/la-liga"
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation Data
              - required: [lang]
              - required: [lang, hasura]
      summary:
        Info [C][H]
      responses:
        200:
          description:
            Target INFO widget data

  # [LP] - STANDINGS Widget
  /api/data/league/standings:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target league/tournament (main) STANDINGS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?league_id=[...]
              league_id:
                type: number
                example: 2
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation;
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Widget Data;
              - required: [league_id]
              - required: [league_id, hasura]
      summary:
        Returns a targets' fixture (id) main Standings widget (data).
      responses:
        200:
          description:
            Target fixture STANDINGS widget data

  # [LP] - FIXTURE-ODDS Widget
  /api/data/league/fix-odds:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target league/tournament (main) STANDINGS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?league_id=[...]
              league_id:
                type: number
                example: 2
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation;
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Widget Data;
              - required: [league_id]
              - required: [league_id, hasura]
      summary:
        Returns a targets' fixture (id) main Standings widget (data).
      responses:
        200:
          description:
            Target fixture STANDINGS widget data

  # [LP] - TOP-PLAYERS Widget
  /api/data/league/top-players:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target league/tournament (main) TOP-PLAYERS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?league_id=[...]
              league_id:
                type: number
                example: 2
              # ?season_id=[...]
              season_id:
                type: number
                example: 2
              # ?lang=[...]
              lang:
                type: string
                example: es
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
            additionalProperties: false
            oneOf:
              # [MAIN] Translation;
              - required: [lang]
              - required: [lang, hasura]
              # [MAIN] Widget Data;
              - required: [league_id]
              - required: [league_id, hasura]
              - required: [league_id, season_id]
              - required: [league_id, season_id, hasura]
      summary:
        Returns a targets' fixture (id) main Top Players widget (data).
      responses:
        200:
          description:
            Target fixture TOP-PLAYERS widget data

  # ===================
  # FIXTURE PAGE
  # ===================

  # [FP] - INCIDENTS Widget
  /api/data/fixture/incidents:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) INCIDENTS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main) [about]
              - required: [fixture_id]
              - required: [fixture_id, hasura]
      summary:
        Returns a targets' fixture (id) main Incidents widget (data).
      responses:
        200:
          description:
            Target fixture INCIDENTS widget data

  # [FP] - PROBABILITY Widget
  /api/data/fixture/probability:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) PROBABILITY widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main) [about]
              - required: [fixture_id]
              - required: [fixture_id, hasura]
      summary:
        Returns a targets' fixture (id) main Probability widget (data).
      responses:
        200:
          description:
            Target fixture PROBABILITY widget data

  # [FP] - ABOUT Widget
  /api/data/fixture/about:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) ABOUT widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main) [about]
              - required: [fixture_id, lang]
              - required: [fixture_id, lang, hasura]
      summary:
        Returns a targets' fixture (id) main About widget (data).
      responses:
        200:
          description:
            Target fixture ABOUT widget data

  # [FP] - STATISTICS Widget
  /api/data/fixture/statistics:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) STATISTICS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main) [scoreboard]
              - required: [fixture_id]
              - required: [fixture_id, hasura]
      summary:
        Returns a targets' fixture (id) main Statistics widget (data).
      responses:
        200:
          description:
            Target fixture STATISTICS widget data

  # [FP] - SCOREBOARD Widget
  /api/data/fixture/scoreboard:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) SCOREBOARD widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main) [scoreboard]
              - required: [fixture_id]
              - required: [fixture_id, hasura]
      summary:
        Returns a targets' fixture (id) main Scoreboard widget (data).
      responses:
        200:
          description:
            Target fixture SCOREBOARD widget data

  # [FP] - CONTENT Widget
  /api/data/fixture/content:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) CONTENT widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main) [content]
              - required: [fixture_id, lang]
              - required: [fixture_id, lang, hasura]
      summary:
        Returns a targets' fixture (id) main Content widget (data).
      responses:
        200:
          description:
            Target fixture CONTENT widget data

  # [FP] - VOTES Widget
  /api/data/fixture/votes:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) VOTES widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?vote=[...]
              vote:
                type: string
                enum:
                  - '1'
                  - '2'
                  - 'X'
                example: "X"
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data [votes]
              - required: [fixture_id]
              - required: [fixture_id, hasura]
              # mutate target [votes]
              - required: [fixture_id, vote]

      summary:
        Returns a targets' fixture (id) main Votes widget (data).
      responses:
        200:
          description:
            Target fixture VOTES widget data

  # [FP] - LINEUP Widget
  /api/data/fixture/lineups:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) LINEUP widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18807082
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main) [lineup]
              - required: [fixture_id]
      summary:
        Returns a targets' fixture (id) main Lineup widget (data).
      responses:
        200:
          description:
            Target fixture LINEUP widget data

  # [FP] - H2H Widget
  /api/data/fixture/h2h:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) H2H widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?teamIds=[...]
              teamIds:
                type: string
                example: 37,57
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main) [lineup]
              - required: [teamIds]
      summary:
        Returns a targets' H2H (team-ids) main H2H widget (data).
      responses:
        200:
          description:
            Target fixture H2H widget data

  # ▓▓ ➤ FP Competition (widget)
  /api/data/fixture/competition:
    get:
      summary:
        Fixture ➤ Competition Widget

      parameters:
        - name: filter
          in: query
          description:
            Fetch target fixture competition widget data.
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ▓▓ ?fixtureId=[...]
              fixtureId:
                type: string
                example: 18946409
              # ▓▓ ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # ▓▓ translation
              - required: [lang]
              - required: [lang, hasura]
              # ▓▓ SEO
              - required: [seo, lang, fixtureId]
              - required: [seo, lang, fixtureId, hasura]
              # ▓▓ data (main)
              - required: [fixtureId]

      responses:
        200:
          description:
            Success

  # ===================
  # 🤾 PLAYER PAGE
  # ===================

  # [PP] - FIXTURES Widget
  /api/data/players/fixtures:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target player (main) FIXTURES widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?player_id=[...]
              player_id:
                type: number
                example: 580
              # ?league_id=[...]
              league_id:
                type: number
                example: 2
              # ?fixture_id=[...]
              fixture_id:
                type: number
                example: 18838496
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main)
              - required: [player_id]
              - required: [player_id, hasura]
              # data (main) (complement)
              - required: [fixture_id]

      summary:
        Returns a targets' player main STATS widget (data).
      description:
        Optional extended description in CommonMark or HTML.
      tags:
        - Player Page
      responses:
        200:
          description:
            Target player FIXTURES widget data

  # [PP] - STATS Widget
  /api/data/players/statistics:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target player (main) STATS widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?player_id=[...]
              player_id:
                type: number
                example: 580
              # ?league_id=[...]
              league_id:
                type: number
                example: 580
              # ?season_id=[...]
              season_id:
                type: number
                example: 580
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main)
              - required: [player_id]
              - required: [player_id, hasura]
              # data (main) (complement)
              - required: [player_id, league_id, season_id]
              - required: [player_id, league_id, season_id, hasura]

      summary:
        Returns a targets' player main STATS widget (data).
      description:
        Optional extended description in CommonMark or HTML.
      tags:
        - Player Page
      responses:
        200:
          description:
            Target player STATS widget data

  # [PP] - SEO Widget
  /api/data/players/seo:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target player (main) SEO widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?player_id=[...]
              player_id:
                type: number
                example: 580
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main)
              - required: [player_id, lang]
      summary:
        Returns a targets' player main SEO widget (data).
      description:
        Optional extended description in CommonMark or HTML.
      tags:
        - Player Page
      responses:
        200:
          description:
            Target player SEO widget data

  # ===================
  # 🤾 USER / PROFILE PAGE
  # ===================

  # [UP] - User Profile
  /api/data/profile.main:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target player (main) SEO widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?uid=[...]
              uid:
                type: string
                example: n65vqAoIH3b7lsU4zroxjHk0SSp2
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              # data (main)
              - required: [uid]
      summary:
        Returns a targets' player main SEO widget (data).
      description:
        Optional extended description in CommonMark or HTML.
      tags:
        - Player Page
      responses:
        200:
          description:
            Target player SEO widget data
    post:
      description:
        Optional extended description in CommonMark or HTML.
      tags:
        - User
      parameters:
        - name: filter
          in: query
          description:
            Target player (main) SEO widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?uid=[...]
              uid:
                type: string
                example: n65vqAoIH3b7lsU4zroxjHk0SSp2
            additionalProperties: false
            oneOf:
              # data (main)
              - required: [uid]
      requestBody:
        description:
          ⚠️❗️ IMPORTANT
          Please read carefully
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/USER-WITHDRAW-REQUEST'
      summary:
        Endpoint dealing with User Updates.
      responses:
        200:
          description:
            User Updated!

  # ===================
  # COMPETITIONS PAGE
  # ===================

  # [CP] - Highlights (lobby)
  /api/data/lobby/highlights:
    get:
      parameters:
        - name: filter
          in: query
          description:
            Target fixture (main) SCOREBOARD widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?competitionIds=[...]
              competitionIds:
                type: string
                example: 175,190
              # ?offset=[...]
              offset:
                type: number
                example: 5
              # ?limit=[...]
              limit:
                type: number
                example: 5
              # ?offset=[...]
              targetStatus:
                type: string
                example: "finished"
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: es
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main)
              - required: [competition_id]
              - required: [competition_id, hasura]
              # data (seo)
              - required: [seo, lang]
              - required: [seo, lang, hasura]
      summary:
        Returns a targets' competition (id) main Scoreboard widget (data).
      responses:
        200:
          description:
            Target competition HIGHLIGHTS widget data

  # [CP] - Main (lobby)
  /api/data/competition/main:
    get:
      summary: Returns a targets' competition (id) main Scoreboard widget (data).

      parameters:
        - name: filter
          in: query
          description: Target fixture (main) SCOREBOARD widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?competition_id=[...]
              competition_id:
                type: number
                example: 23
              # ?seo=[...]
              seo:
                type: boolean
                example: true
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: "es"
              # ?participantUid=[...]
              participantUid:
                type: string
                example: "h1231h23k124h123asd"
              # ?predictionChoice=[...]
              predictionChoice:
                type: string
                example: "yes"
                enum:
                  - "yes"
                  - "no"
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]
              # data (main)
              - required: [competition_id]
              - required: [competition_id, hasura]
              # data (seo)
              - required: [seo, lang]
              - required: [seo, lang, hasura]

      responses:
        200:
          description: Target competition HIGHLIGHTS widget data

  # [CP] - Rules (widget)
  /api/data/competition/rules:
    get:
      summary: Returns a targets' competition (id) main Scoreboard widget (data).

      parameters:
        - name: filter
          in: query
          description: Target fixture (main) SCOREBOARD widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              # ?hasura=[...]
              hasura:
                type: boolean
                example: true
              # ?lang=[...]
              lang:
                type: string
                example: "es"
            additionalProperties: false
            oneOf:
              # translation
              - required: [lang]
              - required: [lang, hasura]

      responses:
        200:
          description: Target competition HIGHLIGHTS widget data

  # ╭──────────────────────────────────────────────────────────────────────────────────╮
  # │ 🎟️ AUTHORS SECTION                                                               │
  # ╰──────────────────────────────────────────────────────────────────────────────────╯

  /api/data/author:
    get:
      summary:
        Returns a targets' competition (id) main Scoreboard widget (data).
      #
      parameters:
        - name: filter
          in: query
          description: Target fixture (main) SCOREBOARD widget data parameters
          required: true
          style: form
          explode: true
          schema:
            type: object
            properties:
              hasura:
                type: boolean
                example: true
              permalink:
                type: string
                example: "betarena-tennis-it/australian-open-men-singles-ryan-peniston-vs-elias-ymer-2023-2024-pronostici-probabili-formazioni-e-consigli-per-le-scommesse-8yomojf34kyzq0j1707239136"
              lang:
                type: string
                example: "es"
            additionalProperties: false
            oneOf:
              # translation
              - required: [permalink]
              - required: [permalink, hasura]
              - required: [lang]
              - required: [lang, hasura]
      #
      responses:
        200:
          description: Target competition HIGHLIGHTS widget data
    #
  #

components:

  schemas:

    USER-WITHDRAW-REQUEST:
      type: object
      properties:
        asset:
          type: string
          example: USD
        quantity:
          type: integer
          example: 53
        amount:
          type: integer
          example: 10
        type:
          type: string
          example: Withdraw
        status:
          type: string
          example: Pending
        Gateway:
          type: string
          example: Stripe
        first_name:
          type: string
          example: M
        last_name:
          type: string
          example: B
        payment_email:
          type: string
          example: '@newEmail'
        iban:
          type: string
          example: 12124123asd12
        bic_swift:
          type: string
          example: asdqwe123123
        wallet_address_erc20:
          type: string
          example: 0xqwe123dasd
        uid:
          type: string
          example: 213210412321sad